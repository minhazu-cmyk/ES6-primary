const firstName = "justin";
const secondName = "trudo";
const fullName = `${firstName} ${secondName} is a good president`;
console.log(fullName);

const multiLine = `i love u,
 i hate u,
  i miss u,
   i dont like u`
console.log(multiLine);